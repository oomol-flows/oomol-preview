version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install && pip install -r requirements.txt
icon: ./icon.png
dependencies:
  oomol-file: 0.0.3
name: oomol-preview
displayName: Preview
description: A set of components to display files and data in various formats
releaseType: package
platform:
  os: ""
releaseNotes: this is the first version
