version: 0.0.3
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    set -e
    pnpm install --frozen-lockfile
icon: ./icon.png
dependencies:
  oomol-file: 0.0.10
  oomol-transform: 0.0.4
name: oomol-preview
displayName: Preview
description: A set of components to display files and data in various formats
platform:
  os: ""
releaseNotes: replace python codes with typescript & upgrade dependencies.
repository: https://github.com/oomol-flows/oomol-preview
